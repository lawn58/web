ƒл€ эксплуатации у€звимости необходима утилита https://jitpack.io/com/github/frohoff/ysoserial/master-SNAPSHOT/ysoserial-master-SNAPSHOT.jar
ѕри получении cookie мы должны заметить то, что куки начинаетс€ с rO0 , это хороша€ заметка о том, что приложение у€звимо к serialised Java objects.
»так, что проэксплуатировать, нам необходимо следующее:
1) запустить приложение с параметром Spring1, сформировать полезную нагрузку и закодировать в base64 без переноса строк, вывод помещаем в текстовый файл, после чего содержимое файла вставл€ем в поле Cookie: auth=
./ysoserial-master-SNAPSHOT.jar Spring1 "/usr/bin/uname" | base64 -w 0 > file.txt
2) после чего отправл€ем на сервер и должны увидеть вывод команды uname.
 
